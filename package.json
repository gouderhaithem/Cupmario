{
  "name": "life-quest",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Life Quest (Pip's Run) — a 2D pixel-art side-scrolling platformer on Canvas 2D.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  }
}
